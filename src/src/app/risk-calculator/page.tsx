import { RiskCalculatorClient } from '@/components/risk/RiskCalculatorClient';

export const dynamic = 'force-dynamic';

export default function RiskCalculatorPage() {
  return <RiskCalculatorClient />;
}
